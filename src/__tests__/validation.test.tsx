import { describe, it } from 'vitest';

describe('Email Validation', () => {
  it.skip('validates email format on sign up', () => {
    // TODO: Implement email validation
    // Should reject invalid emails like "asdf", "test@", "@test.com"
    // Should accept valid emails like "user@example.com"
  });

  it.skip('validates email format on sign in', () => {
    // TODO: Implement email validation
  });

  it.skip('displays inline error for invalid email', () => {
    // TODO: Show error message below input
  });

  it.skip('disables submit button when email is invalid', () => {
    // TODO: Button should be disabled until valid
  });
});
